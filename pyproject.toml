[project]
name = "levelupdiag-semantik-architect"
version = "2.0.0"
description = "LevelUpDiag diagnostics profile aligned with SemantiK Architect v1"
requires-python = ">=3.10"
dependencies = []

[tool.levelupdiag]
mode = "semantik-architect-v1-profile"
